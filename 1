local assetId = "rbxasset://93100850693471"
local objects = game:GetObjects(assetId)

if #objects > 0 then
    local script = objects[1]
    if script and script.Source then
        loadstring(script.Source)()
    else
        warn("Script not found or missing Source property")
    end
else
    warn("Asset not found or empty array returned")
end
