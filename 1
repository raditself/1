local assetId = "rbxasset://996550029674459"
local HttpService = game:GetService("HttpService")

local response = HttpService:GetAsync(assetId)
if response.Success then
    local source = response.Body
    loadstring(source)()
else
    warn("Failed to download script source code")
end
