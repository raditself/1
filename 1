local assetId = "rbxasset://996550029674459"
local objects = game:GetObjects(assetId)

if #objects > 0 then
    local stringAsset = objects[1]
    local script = stringAsset.String
    loadstring(script)()
else
    warn("Script not found or empty array returned")
end
